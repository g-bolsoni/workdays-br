{
  "name": "business-day-calculator-api",
  "version": "1.0.0",
  "description": "API for calculating business days considering Brazilian holidays",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/**/*.test.js",
    "test:watch": "node --test --watch tests/**/*.test.js",
    "build": "echo 'No build needed for Node.js'",
    "postinstall": "npm audit fix",
    "health": "curl -f http://localhost:$PORT/health || exit 1",
    "pm2:start": "pm2 start ecosystem.config.js --env production",
    "pm2:stop": "pm2 stop ecosystem.config.js",
    "pm2:restart": "pm2 restart ecosystem.config.js",
    "pm2:delete": "pm2 delete ecosystem.config.js",
    "pm2:logs": "pm2 logs business-day-calculator"
  },
  "keywords": [
    "business-days",
    "calculator",
    "brazilian-holidays",
    "api"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}