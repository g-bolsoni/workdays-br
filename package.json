{
  "name": "workdays-br",
  "version": "1.0.0",
  "description": "Calculadora de dias úteis considerando feriados nacionais brasileiros",
  "type": "module",
  "scripts": {
    "build": "echo 'Build handled by Vercel'",
    "test": "node --test tests/**/*.test.js",
    "test:watch": "node --test --watch tests/**/*.test.js",
    "deploy": "vercel --prod",
    "deploy:preview": "vercel"
  },
  "keywords": [
    "business-days",
    "calculator",
    "brazilian-holidays",
    "vercel",
    "serverless"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
