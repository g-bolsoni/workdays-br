{
  "name": "calcular-prazo-frontend",
  "version": "1.0.0",
  "description": "Frontend para cálculo de datas considerando dias úteis e feriados nacionais.",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo 'No tests yet'",
    "deploy": "npm run build && vercel --prod",
    "deploy:vercel": "vercel --prod",
    "lint": "echo 'No linting configured'",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "vite": "^4.5.14"
  }
}